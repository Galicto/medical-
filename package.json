{
  "name": "medlife-hms",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install && npx prisma generate",
    "build": "cd frontend && npm install && npm run build"
  }
}
